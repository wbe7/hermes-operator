# TEMPLATE ONLY. Replace both unmistakable sample strings before applying.
# The Secret must be in the same namespace as the Hermes resource.
apiVersion: v1
kind: Secret
metadata:
  name: maria-hermes-secret
  namespace: hermes-users
type: Opaque
stringData:
  TELEGRAM_BOT_TOKEN: REPLACE_WITH_A_DEDICATED_TELEGRAM_BOT_TOKEN
  MODEL_API_KEY: REPLACE_WITH_THE_INSTALLATION_MODEL_API_KEY
